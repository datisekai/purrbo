# Purrbo — Caddy reverse proxy + HTTPS tự động (Let's Encrypt).
# purrbo.fun & www → API (landing / privacy / terms / admin / /v1 đều do api phục vụ).
# Đổi email để nhận cảnh báo gia hạn cert (tuỳ chọn).

{
	email datly3494@gmail.com
}

purrbo.fun, www.purrbo.fun {
	encode gzip
	reverse_proxy api:8000
}
