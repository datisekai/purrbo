node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Backend Python
api/.venv/
**/__pycache__/
*.db
data/

# Secrets / env (KHÔNG commit)
.env
api/.env

# Native — sinh lại từ app.json qua prebuild (CNG). targets/ giữ lại (nguồn widget).
/ios
/android

# apple-targets sinh mỗi prebuild — giữ nguồn PurrboWidget.swift + expo-target.config.js
targets/*/generated.entitlements
targets/*/Info.plist
